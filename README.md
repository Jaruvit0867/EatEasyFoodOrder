# 🍛 ระบบสั่งอาหารด้วยเสียง (Voice-Controlled Ordering System)

ระบบสั่งอาหารสำหรับร้านข้าวแกง ใช้เสียงภาษาไทยในการสั่งอาหาร

![Demo](demo-screenshot.png)

## 📋 คุณสมบัติ (Features)

- 🎤 **สั่งอาหารด้วยเสียงภาษาไทย** - ใช้ faster-whisper สำหรับ Speech-to-Text
- 🤖 **AI วิเคราะห์คำสั่ง** - ใช้ Google Gemini แปลงคำพูดเป็นออเดอร์
- 📱 **ออกแบบสำหรับ Tablet** - UI ที่ใช้งานง่าย ปุ่มใหญ่
- 🎵 **แสดง Waveform** - เห็นภาพคลื่นเสียงขณะพูด
- ✅ **ยืนยันออเดอร์** - ตรวจสอบก่อนบันทึก
- 💾 **บันทึกลง SQLite** - เก็บประวัติออเดอร์ทั้งหมด

## 🛠️ ข้อกำหนดระบบ (Prerequisites)

- **Python** 3.10 หรือสูงกว่า
- **Node.js** 18 หรือสูงกว่า
- **Google API Key** สำหรับ Gemini API

## 🚀 การติดตั้ง (Installation)

### 1. Clone โปรเจค

```bash
git clone <repository-url>
cd EatEasyFoodOrder
```

### 2. ตั้งค่า Backend

```bash
# เข้าสู่โฟลเดอร์ backend
cd backend

# สร้าง virtual environment (แนะนำ)
python3 -m venv venv
source venv/bin/activate  # macOS/Linux
# หรือ .\venv\Scripts\activate  # Windows

# ติดตั้ง dependencies
pip install -r requirements.txt

# ตั้งค่า Google API Key
export GOOGLE_API_KEY="your-api-key-here"

# รัน server
python main.py
# หรือ
uvicorn main:app --host 0.0.0.0 --port 8000
```

> ⚠️ **หมายเหตุ**: ครั้งแรกที่รัน ระบบจะดาวน์โหลด Whisper model (~1.5GB)

### 3. ตั้งค่า Frontend

```bash
# เปิด terminal ใหม่ แล้วเข้าสู่โฟลเดอร์ frontend
cd frontend

# ติดตั้ง dependencies
npm install

# (ถ้าต้องการเชื่อมต่อจาก Tablet) สร้างไฟล์ .env.local
echo "NEXT_PUBLIC_BACKEND_URL=http://<YOUR_LOCAL_IP>:8000" > .env.local

# รัน development server
npm run dev
```

## 🌐 การเชื่อมต่อจาก Tablet

### หาค่า IP Address ของเครื่อง

**macOS:**
```bash
ipconfig getifaddr en0
```

**Linux:**
```bash
hostname -I | awk '{print $1}'
```

**Windows:**
```bash
ipconfig
# ดูที่ IPv4 Address
```

### ตั้งค่า Frontend

1. สร้างไฟล์ `frontend/.env.local`:
```
NEXT_PUBLIC_BACKEND_URL=http://192.168.1.xxx:8000
```

2. รีสตาร์ท frontend server

3. เปิด Browser บน Tablet แล้วเข้า:
```
http://192.168.1.xxx:3000
```

## 🍽️ เมนูที่รองรับ

| เมนู | ราคา (บาท) |
|------|------------|
| ข้าวแกงเขียวหวานไก่ | 50 |
| ข้าวแกงเผ็ดเป็ด | 60 |
| ข้าวแกงมัสมั่นไก่ | 55 |
| ข้าวแกงพะแนงหมู | 50 |
| ข้าวผัดกระเพราหมูสับ | 45 |
| ข้าวผัดกระเพราไก่ | 45 |
| ข้าวไข่เจียว | 35 |
| ข้าวหมูทอดกระเทียม | 50 |
| ข้าวหมูกรอบ | 55 |
| ข้าวมันไก่ | 50 |
| ข้าวขาหมู | 55 |
| น้ำเปล่า | 10 |
| น้ำอัดลม | 15 |
| ชาเย็น | 25 |
| กาแฟเย็น | 30 |

## 🎤 ตัวอย่างการสั่ง

- "ขอข้าวแกงเขียวหวานไก่หนึ่งจาน"
- "ข้าวกระเพราหมูสองจาน ไม่เอาผักชี"
- "ข้าวมันไก่สามที่ กับน้ำเปล่าสามขวด"
- "ขอข้าวไข่เจียวเพิ่มไข่"

## 📡 API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/` | Health check |
| GET | `/menu` | รายการเมนูทั้งหมด |
| POST | `/process-voice-order` | ประมวลผลเสียง → ออเดอร์ |
| POST | `/confirm-order` | บันทึกออเดอร์ลง DB |
| GET | `/orders` | ดูประวัติออเดอร์ทั้งหมด |

## 🔧 Troubleshooting

### Backend ไม่ start

1. ตรวจสอบว่าตั้ง `GOOGLE_API_KEY` แล้ว
2. ลองติดตั้ง dependencies ใหม่: `pip install -r requirements.txt`

### Tablet เชื่อมต่อไม่ได้

1. ตรวจสอบว่าเครื่องและ Tablet อยู่ใน network เดียวกัน
2. ตรวจสอบว่า Firewall ไม่บล็อก port 8000 และ 3000
3. ตรวจสอบว่าตั้ง IP ถูกต้องใน `.env.local`

### ไมโครโฟนไม่ทำงาน

1. อนุญาตการใช้ไมโครโฟนใน Browser
2. ใช้ HTTPS หรือ localhost (HTTP บน IP อื่นอาจไม่ได้รับอนุญาต)

## 📁 โครงสร้างโปรเจค

```
EatEasyFoodOrder/
├── backend/
│   ├── main.py              # FastAPI server
│   ├── requirements.txt     # Python dependencies
│   └── orders.sqlite        # Database (auto-created)
├── frontend/
│   ├── src/
│   │   └── app/
│   │       ├── page.tsx     # Main voice ordering UI
│   │       ├── layout.tsx   # Root layout with Thai font
│   │       └── globals.css  # Custom styles
│   ├── package.json
│   └── .env.local           # Backend URL config
└── README.md
```

## 📄 License

MIT License
