# 📖 คู่มือการติดตั้งระบบ EatEasy (สำหรับผู้เริ่มต้น)

คู่มือนี้สำหรับติดตั้งโปรแกรมลงในเครื่องคอมพิวเตอร์เครื่องใหม่ (Mac/Linux) แบบง่ายๆ ครับ

---

## 🛠️ 1. สิ่งที่ต้องติดตั้งก่อน (Prerequisites)
ก่อนเริ่ม ต้องแน่ใจว่าเครื่องมีโปรแกรมพื้นฐานเหล่านี้ (ถ้ามีแล้วข้ามได้เลย):

1.  **Git**: สำหรับโหลดโค้ด
    *   *วิธีเช็ค*: เปิด Terminal พิมพ์ `git --version`
    *   *ถ้าไม่มี*: [ดาวน์โหลด Git](https://git-scm.com/downloads)
2.  **Node.js**: สำหรับรันหน้าเว็บ
    *   *วิธีเช็ค*: พิมพ์ `node -v`
    *   *ถ้าไม่มี*: [ดาวน์โหลด Node.js (LTS Version)](https://nodejs.org/en)
3.  **Python**: สำหรับรันระบบหลังบ้าน
    *   *วิธีเช็ค*: พิมพ์ `python3 --version`
    *   *ถ้าไม่มี*: [ดาวน์โหลด Python](https://www.python.org/downloads/)

---

## 📥 2. โหลดโปรแกรม (Clone)
เปิด **Terminal** แล้วพิมพ์คำสั่งตามนี้ทีละบรรทัด:

```bash
# 1. ไปที่ Desktop (หรือโฟลเดอร์ที่ต้องการเก็บ)
cd Desktop

# 2. โหลดโค้ดมาจาก GitHub (เปลี่ยน URL เป็นของคุณ)
git clone https://github.com/YOUR_GITHUB_USER/EatEasyFoodOrder.git

# 3. เข้าไปในโฟลเดอร์โปรแกรม
cd EatEasyFoodOrder
```

---

## ⚙️ 3. ติดตั้งครั้งแรก (Setup)
*ทำแค่ครั้งเดียวตอนโหลดมาใหม่ๆ*

ใน Terminal (ที่อยู่ในโฟลเดอร์ `EatEasyFoodOrder`) ให้พิมพ์:

```bash
./easy_setup.sh
```

รอมันทำงานจนขึ้นว่า **✅ Setup Complete!**

---

## 🚀 4. เปิดใช้งาน (Run)
ทุกครั้งที่จะเปิดร้าน ให้พิมพ์คำสั่งนี้:

```bash
./easy_run.sh
```

เมื่อระบบรันเสร็จ มันจะบอก IP Address ให้:
```text
🌐 System is RUNNING!
---------------------------------------------------
👉  Tablet/Mobile Access:  https://192.168.1.5:3000
👉  PC/Local Access:       https://localhost:3000
---------------------------------------------------
```

**วิธีเข้าใช้บน Tablet:**
1.  เปิด Chrome บน Tablet (Android) หรือ Safari (iPad)
2.  พิมพ์ที่อยู่ตามช่อง **Tablet/Mobile Access** (เช่น `https://192.168.1.5:3000`)
3.  ถ้าเจอหน้าจอแดงๆ (Your connection is not private) ให้กด **Advanced** -> **Proceed (unsafe)**
4.  ใช้งานได้เลย!

---

## ❌ หยุดโปรแกรม
กดปุ่ม `Control + C` ในหน้า Terminal เพื่อปิดระบบครับ
